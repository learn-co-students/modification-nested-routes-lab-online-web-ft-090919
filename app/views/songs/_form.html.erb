<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= artist_id_fields %><br />

  <!-- first code attempt below, this works too -->
  <!-- <%= f.label :artist_name %>
  <%= f.collection_select :artist_id, Artist.all, :id, :name %> -->


  <%= f.submit %>
<% end %>
